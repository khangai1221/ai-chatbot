/**
 * Prisma 7 configuration file.
 * This file provides datasource connection URLs and other settings
 * so that `prisma` CLI commands like `db push` and `migrate` can run.
 *
 * It intentionally reads connection values from environment variables.
 */

export default {};
